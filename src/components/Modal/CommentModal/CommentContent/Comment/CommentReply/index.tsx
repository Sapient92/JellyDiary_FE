import { CommentReplyContainer } from "./CommentReply.styled.ts";

const CommentReply = () => {
  return (
    <CommentReplyContainer>
      <p>이건 답글입니다.</p>
    </CommentReplyContainer>
  );
};

export default CommentReply;
